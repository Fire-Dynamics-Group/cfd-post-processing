[pytest]
markers =
    slow: tests that take >1s (e2e against real FDS run dirs, subprocess spawning)
# Default-skip slow tests so the TDD inner loop stays fast.
# Run them deliberately with: pytest -m slow tests/
addopts = -m "not slow"
