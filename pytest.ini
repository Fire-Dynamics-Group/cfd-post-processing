[pytest]
markers =
    slow: tests that take >1s (e2e against real FDS run dirs, subprocess spawning)
# Slow tests run by default — the e2e test against the Finchley fixture
# (~60s) catches regressions that mock-based tests miss. Skip them
# during the TDD inner loop with: pytest -m "not slow" tests/
